<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rabiayel.healthylife_3">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/uylogo"
        android:label="healthylife"
        android:supportsRtl="true"
        android:theme="@style/UygulamaTeması">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ForgetPassword" />
        <activity android:name=".NewPassword" />
        <activity android:name=".MainActivity" />
        <activity android:name=".AcilMudahale" />
        <activity android:name=".MudahaleEdilen" />
        <activity android:name=".SetMudahaleEdilen"></activity>
    </application>

</manifest>